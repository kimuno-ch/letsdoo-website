{
	"name": "letsdoo/letsdoo-website",
	"description": "Development-only tooling for the Let's Doo WordPress site. These packages are IDE/static-analysis stubs and are never loaded at runtime — only ./wp-content/themes is mounted into the container, so vendor/ never reaches PHP.",
	"type": "project",
	"license": "proprietary",
	"require-dev": {
		"php-stubs/wordpress-stubs": "^7.0",
		"php-stubs/acf-pro-stubs": "^6.8"
	}
}
